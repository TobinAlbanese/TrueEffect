# Security Notes

The web app must keep service-role keys server-only, use safe DTOs, rate limit sensitive actions, and respect RLS boundaries.

This is a planning document only. Implementation should wait until the architecture skeleton is approved.
