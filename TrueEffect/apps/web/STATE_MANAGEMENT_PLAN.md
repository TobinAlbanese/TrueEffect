# State Management Plan

MVP state should stay simple: server-derived data, route state, form state, and small client state islands where needed.

This is a planning document only. Implementation should wait until the architecture skeleton is approved.
