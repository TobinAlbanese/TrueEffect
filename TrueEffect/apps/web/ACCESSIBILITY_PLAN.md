# Accessibility Plan

Accessibility should include semantic structure, keyboard support, visible focus states, color contrast, readable type, and reduced-motion support.

This is a planning document only. Implementation should wait until the architecture skeleton is approved.
