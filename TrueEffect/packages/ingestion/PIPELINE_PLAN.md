# Pipeline Plan

The future pipeline checks sources, fetches public documents, saves raw records, extracts text, normalizes metadata, deduplicates by checksum, detects events, and sends candidates to AI and review.
