# Structured Output Plan

AI outputs should be structured records with required source IDs, uncertainty labels, confidence labels, risk labels, reviewer notes, and validation status. No executable prompt files are included.
