# Security Package Plan

Future security work should cover input validation, output encoding, CSRF considerations, rate limiting, server-only secrets, dependency scanning, admin audit logs, RLS tests, storage policies, prompt injection mitigation, and privacy-by-design.
