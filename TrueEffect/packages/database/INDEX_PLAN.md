# Index Plan

Future indexes should support owner-scoped profile reads, feed queries by user and recency, published content by jurisdiction and life area, source checks by source and time, document checksums, review queue status, issue updates, watchlist lookups, and audit log filtering. Postgres full-text indexes should come before semantic search indexes.
