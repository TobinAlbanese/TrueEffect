# Background Jobs

Recommended primary: Trigger.dev. Alternatives include Inngest for event-driven durable workflows, Upstash QStash for lightweight scheduled HTTP jobs, and Temporal later if enterprise complexity requires it. No code exists here.

## Architecture-Only Status
This folder is part of the initial repository architecture pass. It contains planning notes only and should not contain executable application code yet.
