# Ingestion Worker

Future jobs include checking the source registry, fetching new documents, extracting text, detecting changes by checksum, creating raw document records, normalizing documents, and enqueueing event detection.

## Architecture-Only Status
This folder is part of the initial repository architecture pass. It contains planning notes only and should not contain executable application code yet.
